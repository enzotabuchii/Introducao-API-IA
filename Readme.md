## | Introdução a Aplicação da IA com a API da OpenAI |
Não se esqueça de importar as libs do python3

```
pip install openai
pip install python-dotenv
```

Além disso, também é preciso adicionar a chave API da OpenAI na .env, pegando baseado na .env.example

